from _maesy_core.model.model_tools.model_factory import create_model

def cluster(model: str, dataset: str):
    """
    Cluster images in a dataset using a specified model.

    Args:
        model (str): The path to the model to use for feature extraction.
        dataset (str): The path to the dataset to cluster.
    """
    if
    model = create_model(model)